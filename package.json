{
  "name": "local-vending-machine",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "build": "npm --prefix admin run build",
    "test": "node --test test/*.test.js",
    "seed:demo": "node --env-file=.env scripts/seed-demo.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.0",
    "helmet": "^8.1.0",
    "multer": "^2.0.2",
    "serialport": "^13.0.0",
    "sharp": "^0.34.0",
    "zod": "^4.0.0",
    "promptpay-qr": "^0.5.0"
  }
}
